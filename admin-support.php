<?php
/*
|--------------------------------------------------------------------------
| این فایل قبلاً یک صفحه‌ی مستقل و ناقص بود (به admin-support.js که اصلاً
| وجود نداشت لینک می‌داد و در نوار تب‌های خودش هم لینک‌های اشتباه داشت).
| بخش پشتیبانی حالا به‌صورت یکپارچه در تب «🎧 پشتیبانی» داخل admin-panel.php
| پیاده‌سازی شده. این فایل فقط برای حفظ سازگاری با لینک‌های قدیمی نگه
| داشته شده و مستقیماً کاربر را به همان تب هدایت می‌کند.
|--------------------------------------------------------------------------
*/

session_start();

if (!isset($_SESSION['is_admin']) || $_SESSION['is_admin'] !== true) {
    header('Location: admin-login.php');
    exit;
}

header('Location: admin-panel.php#support');
exit;
